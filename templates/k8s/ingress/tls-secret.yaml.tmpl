# TLS secret for {{ .Domain }}
# This secret is created automatically by setup.sh using mkcert.
# To regenerate manually:
#   mkcert -key-file key.pem -cert-file cert.pem {{ .Domain }} "*.{{ .Domain }}"
#   kubectl create secret tls {{ .ProjectName }}-tls \
#     --cert=cert.pem --key=key.pem \
#     -n {{ .ProjectName }}
